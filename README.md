# Quickstart Onboarding for Angular

## Install
1. Run `npm install ng-onboarding-quickstart`.
2. Import the `OnboardingModule.forRoot(config)` in your `AppModule`. The config object is optional.
3. 
