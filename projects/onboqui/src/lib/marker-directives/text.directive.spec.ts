import { TextContainerDirective } from './text-container.directive';

describe('TextContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextContainerDirective();
    expect(directive).toBeTruthy();
  });
});
