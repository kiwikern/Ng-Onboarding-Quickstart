import { OnboardingDirective } from './onboarding.directive';

describe('OnboardingDirective', () => {
  it('should create an instance', () => {
    const directive = new OnboardingDirective(null, null, null, null);
    expect(directive).toBeTruthy();
  });
});
